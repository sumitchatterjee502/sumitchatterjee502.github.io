# Local dev (npm run dev): POST /api/send-email
GMAIL_USER=you@gmail.com
GMAIL_APP_PASSWORD=your-app-password

# Optional: test the Render API locally instead of /api/send-email
# NEXT_PUBLIC_CONTACT_API_URL=http://localhost:8787

# GitHub Actions secret (production static site on Pages):
# CONTACT_API_URL=https://your-service.onrender.com
